local fs=require("filesystem")
local args={...}
local function printUsage()
    print("umount \"Disk\"")
end

if not fs.isDrive(args[1]) then
    printUsage()
else
    fs.removeMountPoint(args[1])
end