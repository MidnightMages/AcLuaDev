local fs=require("filesystem")
local shell=require(("shell"))
local args={...}
if args[1]==nil then
    for i,v in ipairs(fs.list(shell.dir())) do
        print(v)
    end
else
    for i,v in ipairs(fs.list(args[1])) do
        print(v)
    end
end