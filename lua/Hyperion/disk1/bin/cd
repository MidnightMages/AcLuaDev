local shell=require("shell")
local args={...}
if args[1]=="." then
elseif args[1]==".." then
    local tokens=string.split(shell.dir(),"/")
    shell.setDir(table.concat(tokens,"/",2,#tokens-2).."/")
elseif args[1]=="~" then
else
    shell.setDir(shell.dir()..args[1].."/")
end